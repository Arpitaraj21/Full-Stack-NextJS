# Technologies

NextJS + Typescript

# Model

1. User -> email, password, \_id (created by mongoDB), createdAt, updatedAt

2. Video -> title, description, videoURL, thumbnailURL, controls (play/pause), transformation

### check condition ->

height, width, quality
